figure out how to have sandboxes so that multiple instances of this website can be running at once (subdomains?)
I would still like to have user setup stuff: everything except the ssh privite key itself
